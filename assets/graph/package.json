{
  "scripts": {
    "build": "esbuild src/* --bundle --minify --sourcemap --format=esm --outdir=precompiled && ../calc_fingerprint.sh precompiled/*.js src package.json package-lock.json"
  },
  "dependencies": {
    "cytoscape-expand-collapse": "^4.1.0",
    "cytoscape-fcose": "^2.2.0"
  },
  "devDependencies": {
    "esbuild": "0.17.11"
  }
}
